/** @type {import('next').NextConfig} */

const nextConfig = {
  publicRuntimeConfig: {
    realtor: {
      name: "Tim Flores",
      brand: "Flores Realty",
    },
  },
};

export default nextConfig;
